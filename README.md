# renovate-config

Shared Renovate configuration for repositories owned by `athal7`.

Repositories extend `github>athal7/renovate-config`.

The policy runs weekly, waits three days after releases, groups updates by package manager, and auto-merges stable minor and patch updates after required checks pass. Pre-1.0 and major updates require review.
